# ================================================================
# Kelvo E-Comm - Local Development Configuration
# ================================================================
#
# HOW TO USE:
#   1. Copy this file:  cp .env.local .env
#   2. Open .env and fill in ALL <YOUR_...> placeholders
#   3. Run:  docker-compose up -d
#
# Docker Compose reads .env automatically from the project root.
# The .env file is git-ignored — your secrets stay local.
#
# This file (.env.local) is the TEMPLATE. Never put real keys here.
# ================================================================

# -------------------------------------------
# AWS Region (used by local AWS SDK calls)
# -------------------------------------------
AWS_REGION=us-east-1

# -------------------------------------------
# Datadog - Agent (backend APM + metrics + DBM)
# -------------------------------------------
# Get your API key:  https://app.datadoghq.com/organization-settings/api-keys
# Get your App key:  https://app.datadoghq.com/organization-settings/application-keys
DD_API_KEY=<YOUR_DATADOG_API_KEY>
DD_APP_KEY=<YOUR_DATADOG_APP_KEY>
DD_SITE=datadoghq.com
DD_ENV=local
DD_VERSION=1.0.0

# -------------------------------------------
# Datadog - RUM (frontend browser monitoring)
# -------------------------------------------
# Create a RUM app:  https://app.datadoghq.com/rum/application/create
# The Application ID and Client Token are on that same page.
#
# REACT_APP_ prefix is required — React injects these at build time.
# Docker Compose and scripts also read these for the dd-agent config.
REACT_APP_DD_APPLICATION_ID=<YOUR_DD_RUM_APPLICATION_ID>
REACT_APP_DD_CLIENT_TOKEN=<YOUR_DD_RUM_CLIENT_TOKEN>
REACT_APP_DD_SITE=datadoghq.com
REACT_APP_DD_SERVICE=kelvo-ecomm
REACT_APP_DD_ENV=local
REACT_APP_DD_VERSION=1.0.0

# -------------------------------------------
# Service Ports (local)
# -------------------------------------------
ORDER_SERVICE_PORT=8080
CART_SERVICE_PORT=3001
AUTH_SERVICE_PORT=3002
PAYMENT_SERVICE_PORT=3003
SEARCH_SERVICE_PORT=3004
RECOMMENDATIONS_SERVICE_PORT=3005
NOTIFICATIONS_SERVICE_PORT=3006
FRONTEND_PORT=3000

# -------------------------------------------
# Service URLs (used by the React frontend)
# -------------------------------------------
REACT_APP_ORDER_API=http://localhost:8080
REACT_APP_CART_API=http://localhost:3001
REACT_APP_AUTH_API=http://localhost:3002
REACT_APP_PAYMENT_API=http://localhost:3003
REACT_APP_SEARCH_API=http://localhost:3004
REACT_APP_RECOMMENDATIONS_API=http://localhost:3005
REACT_APP_NOTIFICATIONS_API=http://localhost:3006

# -------------------------------------------
# PostgreSQL (local Docker container)
# -------------------------------------------
POSTGRES_DB=rumshop
POSTGRES_USER=rumshop
POSTGRES_PASSWORD=rumshop

# -------------------------------------------
# Auth / JWT
# -------------------------------------------
# For local dev, any string works. For production, generate one:
#   openssl rand -hex 32
JWT_SECRET=rum-shop-local-secret-key-2024
