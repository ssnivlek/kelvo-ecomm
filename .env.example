# ================================================================
# RUM Shop - Quick Reference
# ================================================================
# This is a minimal example. For full configuration, see:
#   .env.local   → local development (Docker Compose)
#   .env.aws     → AWS deployment
#
# Usage:
#   cp .env.local .env      # for local dev
#   cp .env.aws .env         # for AWS deploy
#   # Then fill in your values in .env
# ================================================================

# Datadog Agent
DD_API_KEY=<YOUR_DATADOG_API_KEY>
DD_APP_KEY=<YOUR_DATADOG_APP_KEY>
DD_SITE=datadoghq.com

# Datadog RUM (Frontend)
REACT_APP_DD_APPLICATION_ID=<YOUR_DD_RUM_APPLICATION_ID>
REACT_APP_DD_CLIENT_TOKEN=<YOUR_DD_RUM_CLIENT_TOKEN>

# Auth
JWT_SECRET=<YOUR_JWT_SECRET>

# Database (AWS only)
RDS_PASSWORD=<YOUR_RDS_PASSWORD>
